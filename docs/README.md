# SmartPOS X — Documentation

> **The Complete Mobile First Business Management Platform.**

This folder is the **single source of truth** for SmartPOS X. Code follows documentation, never
the reverse. If code and documentation disagree, that is a defect in one of them and it is fixed
in the same pull request.

---

## How to use this documentation

| If you want to… | Read |
| --- | --- |
| Understand what we are building and why | [01 — Project Vision](01-vision/) |
| Understand how we work and what "done" means | [00 — Governance](00-governance/) |
| Understand why a technical choice was made | [Architecture Decision Records](adr/) |
| Look up a term | [Glossary](GLOSSARY.md) |
| See what is built and what is not | [Module Registry](modules/) |

---

## Phase map

Phases run in order. **No phase is skipped.** A phase is not started until the previous phase is
approved. Each phase folder contains a `README.md` charter defining its objective, inputs,
deliverables and exit criteria.

| # | Phase | Folder | Status |
| --- | --- | --- | --- |
| 01 | Project Vision | [01-vision](01-vision/) | 🟢 Approved (OD-01 launch market provisional — see [open-decisions.md](01-vision/open-decisions.md)) |
| 02 | Business Requirements | [02-business-requirements](02-business-requirements/) | 🔵 In review — 54 BRs drafted; blocked from 🟢 only by GST-practitioner review and OD-01 confirmation |
| 03 | Functional Requirements | [03-functional-requirements](03-functional-requirements/) | 🔵 In review — 84 FRs, 26 NFRs, 26 domain rules, 28 user stories drafted |
| 04 | Software Requirement Specification | [04-srs](04-srs/) | 🔵 In review — SRS, system context (7 trust boundaries), constraints, quality attributes, dependencies all drafted |
| 05 | User Personas | [05-personas](05-personas/) | 🟡 Draft — hard-blocked on real shop interviews, not just research (see phase notes) |
| 06 | Business Workflows | [06-workflows](06-workflows/) | 🔵 In review — 13 V1 workflows, 4 exhaustive state machines, 3 findings flagged for Phase 13 |
| 07 | Database Design | [07-database](07-database/) | 🔵 In review — 25 tables (22 original + 3 added during implementation, never backported until Sprint 69's audit), 21 actually built, 6 ADRs accepted, stock-ledger correctness proven on paper |
| 08 | Folder Structure | [08-folder-structure](08-folder-structure/) | 🔵 In review — layering rules designed (CI enforcement, `import-boundaries`, not yet built — `ci-workflows.md` and `layering-rules.md` both already name this; corrected here Sprint 75, this row previously overclaimed "CI-enforced"), both apps mapped to Phase 07's schema; the OpenAPI contract-generation mechanism (`shared-contracts.md`) was designed but never built, corrected Sprint 75 |
| 09 | Navigation | [09-navigation](09-navigation/) | 🔵 In review — 42 routes mapped, tap-count audit found and fixed 1 over-budget workflow |
| 10 | Design System | [10-design-system](10-design-system/) | 🔵 In review — contrast proven by measurement in both themes; 3-screen composition proof; physical printer/device testing flagged pending for Phase 14/18 |
| 11 | API Design | [11-api](11-api/) | 🔵 In review — full contract for 7 modules + sync/realtime; 7 of 8 exit criteria met, 10× load test tracked to Phase 14/16 |
| 12 | Security Design | [12-security](12-security/) | 🔵 In review — full STRIDE across 8 boundaries, 22-table tenant-isolation checklist, cross-tenant proof extended to Realtime |
| 13 | Offline Synchronisation | [13-offline-sync](13-offline-sync/) | 🔵 In review — idempotent-replay and concurrent-composition proofs; all 10 failure scenarios resolved; all 3 standing cross-phase gaps (Finding 1, Realtime-outage, storage-full) closed |
| 14 | Testing Strategy | [14-testing](14-testing/) | 🔵 In review — full 26-rule test traceability; 3-tier CI pipeline; found and fixed a DR-025 audit-logging gap from Phase 12 |
| 15 | GitHub Project | [15-github-project](15-github-project/) | 🔵 In review — branch protection, 3-tier CI/CD workflows, full DoD-enforcing PR template; rollback rehearsal tracked to Phase 18 |
| 16 | Milestones | [16-milestones](16-milestones/) | 🔵 In review — all 5 deliverables complete; OD-06 resolved (solo, 10–20 hrs/week); V1 converts to ~10–15 months at the midpoint pace |
| 17 | Sprint Planning | [17-sprints](17-sprints/) | 🔵 In review — 2-week solo-sized cadence; M0 fully decomposed (21 person-days); M1 fully decomposed 2026-08-14 (8 items, 15.5 person-days) and **fully closed**; M2 fully decomposed 2026-08-14 (6 items, 12 person-days) and **fully closed**; M3 fully decomposed 2026-08-16 (5 items, 13.5 person-days) and **fully closed**; M4 fully decomposed 2026-08-16 (9 items, 19.5 person-days), items 1–8 done, item 9 remaining; **M5 fully decomposed 2026-08-21 (7 items, zero engineering — "no new product scope"), a dated exception begun ahead of M4's formal closure by explicit founder direction**; Sprint 66/67/68 (2026-08-25, repository-tooling fixes, not milestone work) triaged 12 long-open Dependabot PRs and closed all three confirmed breaking changes (`eslint-config-next` 16, Vitest 4, Prisma 7); Sprint 69 (2026-08-25, documentation-only) re-audited Phase 07's `schema-server.md` against the live schema, correcting a 42-sprint-old false `device_id` design claim and adding 3 undocumented real tables; Sprint 70 (2026-08-26) built the 2 of Sprint 69's 4 named missing indexes that were real, after re-verification caught one that wasn't; Sprint 71 (2026-08-26) triaged a new weekly Dependabot batch (7 PRs) — 5 merged clean, 2 left open on confirmed genuine upstream blockers (`typescript` 7.0, `eslint` 10.9.0); Sprint 72 (2026-08-26) built the `pg_trgm` trigram GIN indexes for product search, the last real gap from Sprint 69's audit besides `hsn_sac_code_at_sale`; Sprint 73 (2026-08-26) built `hsn_sac_code_at_sale`, closing every real finding from Sprint 69's audit; Sprint 74 (2026-08-26, documentation-only) found `authentication/specification.md` unchanged since Sprint 06 despite Sprints 23/55/56 building nearly everything it still described as unbuilt, corrected comprehensively; Sprint 75 (2026-08-26, documentation-only) found the OpenAPI-driven contract-generation mechanism `shared-contracts.md` designs was never built across 74+ sprints, corrected across four documents with the explicit call not to build it now; Sprint 76 (2026-08-26, documentation-only) found a real false safety claim (`trading_days` "conflict-free by construction" via per-device scoping, never true) repeated across three documents, corrected everywhere; Sprint 01 through Sprint 76 all closed |
| 18 | Implementation | [18-implementation](18-implementation/) | 🟡 In progress — Sprints 01–15 built the full M0 walking skeleton (Identity/Auth through Bluetooth receipt printing), each closing a real gap first before writing code; Sprint 16 ran M0's own end-to-end proof for real on the founder's device — steps 1–7 (sign in, add a product, sell offline, reconnect, sync) confirmed working, no bug found, with only the physical-print step left open pending printer hardware the founder doesn't yet own; the founder then directed M1 to begin regardless (modules/README.md Rule 2's third exception); Sprint 17/18 built the first two M1 modules (Categories and Units), Sprint 19 extended Products with `category_id`/`unit_id`/`sku`/`barcode`/`hsn_sac_code` (optional, not required), Sprint 20 built the mobile catalogue UI (first local schema migration; category/unit creation found to be online-only), Sprint 21 built `GET /api/v1/products` plus the till's own barcode-scan/search/category-filter (all local, per FR-034/FR-036's "Fully offline" classification) — fixing a second real gap along the way, a Sprint 20 sync-pull mapping that never carried category/unit/sku/barcode down to devices — Sprint 22 built the `adjustment` stock-movement type plus the public `stock-movements`/`stock-balance` endpoints, finding that the create endpoint should exclude `movement_type: 'opening'` entirely rather than the originally-documented "accept it" contract, since every product already gets one automatically at creation, Sprint 23 built Roles & Permissions in full — `user_store_roles`, `GET/POST/PATCH/DELETE /users*`, `GET /audit-log`, and permission enforcement retrofitted across every endpoint built so far — closing three real gaps along the way (onboarding never assigned a role, `GET /audit-log` was named as a retrofit target but never built, and `POST /users/invite`'s original mechanism was underspecified, resolved via Supabase Admin's synchronous identity creation), and Sprint 24 closed M1 in full — canonical invoice numbers (an atomic per-tenant-per-financial-year counter, ADR-0008) and `GET /sales/{id}`/`GET /sales`/`GET /sales/lookup`, applying Sprint 23's own routing lesson proactively; GST invoice fields remain named, deferred M2 scope, since no tax computation exists yet. **M1 — Full Catalogue & Inventory, Multi-Role is fully built, all 8 backlog items done.** Sprint 25 opened **M2 — Full POS Loop** with its first item, Settings — `shop_settings` (tax mode/rate, pricing mode, rounding rule, auto-approval thresholds), a default row now written at onboarding, `GET`/`PATCH /api/v1/settings` role-shaped and optimistically-concurrent — closing two real gaps found while decomposing M2 itself: no `shop_settings` row was ever created anywhere in code, and neither schema-server.md nor money-and-tax.md ever named where the tax rate actually comes from (resolved as a dated Phase 07 correction, a single shop-wide flat rate). Sprint 26 built M2 item 2, Cash Drawer / Trading Day — `trading_days`, `POST /trading-days/open`/`{id}/close`/`{id}/reopen`, `GET /trading-days/current`, scoped per-store rather than per-device (no `devices` table exists) or per-user (this item's own pre-sprint guess), reasoned instead from offline-workflows.md's own already-recorded Finding 2 text; built the reopen endpoint, closing a real gap sales.md never listed despite state-machines.md/audit-model.md already specifying it; deliberately deferred `POST /sales`'s `TRADING_DAY_NOT_OPEN` hard gate, reversing this item's own pre-sprint plan, to avoid regressing the one live, working end-to-end sale flow this project has ahead of the matching mobile till change. Sprint 27 built M2 item 3, Discount — per-line `discount_percent_basis_points`/`discount_amount_minor_units` (DR-011), `DISCOUNT_REQUIRES_APPROVAL` above threshold (DR-012) satisfied by the caller's own Manager/Owner role or a named `discount_approved_by`, resolved fresh at request time per the same integrity model Finding 1 already established for offline approvals; found and corrected a real gap in the same pass — `sales.subtotal_minor_units` had silently meant "pre-discount raw sum" since M0, not money-and-tax.md's always-specified post-discount value, invisible until Discount existed to make the two diverge. Sprint 28 built M2 item 4, Tax computation — `tax_total_minor_units`/`tax_registration_type_at_sale`/per-line `tax_rate_basis_points`/`tax_minor_units`, wired entirely from `shop_settings` (no new request field), both exclusive and inclusive pricing modes; found and resolved a real gap in money-and-tax.md's own two worked examples in the same pass — inclusive pricing combined with a discount on the same line was never jointly specified, resolved as the natural composition of two already-accepted rules. Sprint 29 built M2 item 5, Split Payment — `payments` loosened to one-or-more entries across `cash`/`card`/`other` (FR-028), `PAYMENT_AMOUNT_MISMATCH` restated as a sum check, no schema change needed; confirmed live that Trading Day's own Sprint 26 aggregation query needed zero changes to already exclude a split sale's card/other portions from `expected_cash_minor_units`. Sprint 30 closed M2's last item, Hold/Resume — mobile-only, no server change at all: `sales.status` now transitions `draft`→`held`→`draft`→`completed` on the client, `completeSale` rewritten to transition the existing draft/held row in place; built to navigation-model.md §4's fuller pre-existing requirement (continuous auto-persistence of the active cart from its first item, not merely a hold button), and corrected a real schema-local.md gap along the way — its "Immutable event" classification was never literally true once a draft/held row is genuinely mutated pre-completion. **M2 — Full POS Loop is fully built, all 6 backlog items done.** M3 — Customers, Returns & Refund, conflict-resolution field-merge — was then fully decomposed to item grain, finding that the sync engine has never had a single `.update` operation type for any entity despite several tables being classified "Client-editable" since Phase 13, and that `offline-workflows.md` Finding 1's on-paper `sync_rejections` resolution was never actually built. Sprint 31 built M3's first item, Customers (server) — `customers` table, `sales.customer_id`, `POST`/`GET`/`PATCH`/`DELETE /customers`, `GET /customers/{id}/purchase-history`, live-verified (12/12); found and fixed a real bug live (a Zod `.refine()` returning the wrong error code) and corrected permission-matrix.md's own missing edit/deactivate-customer rows in the same pass. Sprint 32 built M3 item 2, Customers (mobile), as a full-stack item rather than mobile-only — `customer.create` added as the sync engine's third push operation type, `POST /sales` gains an optional server-validated `customer_id`, and the mobile `CustomerPickerSheet` (a bottom sheet over the till, not a route push — FR-050's own wording taken literally) plus full `/customers`/`/customers/:id` browse routes, the attached customer surviving hold/resume (FR-026); live-verified (9/9 server checks, 145/145 `flutter test`). Sprint 33 built M3 item 3, Returns & Refund (server) — `returns`/`return_line_items` tables, `POST`/`GET /returns*`, `POST /returns/{id}/approve`/`reject`, and `return.create`/`return.approve`/`return.reject` as the sync engine's fourth through sixth push operation types; found and corrected two real gaps in schema-server.md's own `returns` table design while writing the spec (a missing `created_by`/`created_at` column pair, and a documented `client_operation_id` column with no working precedent anywhere else in this schema, dropped in favour of `id` alone); resolved DR-014's per-unit-price rounding ambiguity as a dated decision (exact-remaining-amount for a full return, proportional rounding only for a genuine partial); live-verified (22/22). Sprint 34 built M3 item 4, Returns & Refund (mobile) — `/returns/new`/`/returns/:id`/`/returns/approvals`, local `Returns`/`ReturnLineItems` tables, `return.create`/`return.approve`/`return.reject` written to `outbound_queue`; found and fixed a real, blocking gap before writing mobile code (`formatSale` never exposed a sale line item's own `id`, which `POST /returns` needs); resolved the approvals-queue badge placement (no Reports tab exists yet, M4) and WF-013's interrupt/queue split (an inline post-creation prompt, no new realtime infrastructure) as dated decisions rather than left implicit; `flutter analyze`/`flutter test` (176 total mobile tests) clean. Sprint 35 closed M3's last item, conflict-resolution field-merge (`customers` only) — `customer.update` as the sync engine's first `.update` operation type of any kind, `PATCH /customers/{id}` upgraded in place to the same merge-aware service function; found and resolved a real gap while writing the spec (`base_updated_at` alone can't support a field-level 3-way merge, resolved via client-supplied per-field base values, no new server-side history mechanism); mobile gains its first customer-edit screen and a conflict-resolution screen; live-verified 18/18, the exact worked-example scenario (two staff editing the same customer's phone number) provoked for real. **M3 — Customers & Returns is now fully closed, all 5 backlog items done**, satisfying milestones.md's own hard exit criteria for a return completing correctly and a field-edit conflict surfacing in business language. M4 — Reports, Settings, and Release Readiness — was then fully decomposed to item grain (9 items, 19.5 person-days), correcting a real self-contradiction found in milestones.md's own M4 row along the way (its Scope line named the 10× load test, which its own Exit criteria row's pilot-ready-only restriction actually excludes). Sprint 36 built M4's first item the same day: `GET /sync/pull` gains `stock_movements`/`sales` entity types — the "reporting parity across devices" pull sync-api.md §6 named since Phase 11 and never implemented — unblocking Reports (M4 item 2, next). Found a real gap while implementing: a single `next_cursor` field can't mean both "keep paging now" and "durable resume point for the next sync cycle" for an ever-growing entity type, resolved with a distinct `has_more` field (a dated correction to sync-api.md §6) and a new local `sync_cursors` table for mobile to persist a per-entity-type resume cursor against — the opposite trade-off from `products`' own deliberately-unchanged full-re-pull-every-cycle behaviour. Live-verified 24/24. Sprint 37 built M4 item 2 the same milestone: all four core reports (daily sales, top products, stock value, low stock), entirely local Drift aggregation, no new server report endpoint. Found two further real gaps while starting it: no low-stock threshold configuration existed anywhere (resolved as a shop-wide `shop_settings.low_stock_threshold_quantity`), and `shop_settings` had never been synced to mobile despite being documented since Phase 11 (resolved as sync pull's fourth entity type). Built this codebase's first genuine client-side role-awareness — a cached, fail-closed probe against the existing `GET /users` endpoint — to gate the Reports entry point, since no network call at report-view time exists to surface a `403` the way every other role-gated screen does. Server additions live-verified 11/11. Sprint 38 closed M4 item 3, Settings mobile UI — `/settings` surfacing tax mode/rate, pricing mode, rounding rule, currency, low-stock threshold, and both auto-approval thresholds; no server change needed (already complete since Sprint 25/37). Built as Pattern B (reachable by every role, the server's own role-shaped response and `403` do the gating) rather than Reports' hide-entirely Pattern A, since `GET /settings` is itself a real, already role-shaping network call. Found a real spec-currency gap while updating the module spec — Sprint 37's `low_stock_threshold_quantity` had silently drifted out of `settings/specification.md`'s own Change Log — corrected in the same pass, along with a `route-map.md` correction. 227 mobile tests (were 218), no new live verification needed. Sprint 39 closed M4 item 4, printer pairing + receipt template — `/settings/printer` (FR-077) and `/settings/receipt-template` (FR-078) built. Corrected this item's own original framing twice: `receipt_template_config` becomes `PATCH`-able with exactly one field, `footer_message`, leaving `RECEIPT_TEMPLATE_MISSING_MANDATORY_FIELD` deliberately unreachable since GSTIN (the only real conditional-mandatory-field candidate) isn't captured in `shop_settings` anywhere; `printer_config` stays untouched, since a paired printer is per-device data resolved instead as a new local-only mobile table, `PairedPrinterCache`, never sent to the server. `ShopSettingsCache` extended with the footer message so printing stays fully offline; the per-sale print action now reuses both. Live-verified 7/7; 239 mobile tests (were 227), 209 web tests (were 207). Sprint 40 closed M4 item 5, the cross-tenant isolation suite, CI-enforced — `pr.yml` gains `fast-integration`, a fresh `postgres:15` container per run, 76 real authenticated negative tests against all 19 real tenant-owned tables, wired as a genuine blocking PR gate for the first time. Found and closed a real, previously-undetected security gap in the same pass: `sale_line_items`/`sale_payments`/`return_line_items` had no row-level security enabled at all, contradicting the schema's own design template — closed with two new migration files, verified by deliberately breaking RLS on one table and confirming the suite catches it. Also corrected a stale "22 tables" figure (5 listed tables were never built, 2 real tables were never listed, one was miscategorised — corrected to 19) and reconciled CI-placement wording that had drifted apart across four documents. The Realtime-channel extension is deliberately deferred, named as a real gap requiring the full local Supabase CLI stack. No local/containerized database infrastructure existed anywhere in this repo before this sprint. 209 web unit tests unaffected (a fully separate suite/config). Sprint 41 closed M4 item 6, the offline adversarial suite, CI-enforced — idempotent-replay (3/3), concurrent-composition non-fuzzed (4/4), and 1 server-testable failure scenario added to the same `fast-integration` job, no toxiproxy or new CI infrastructure needed since replay-safety and order-independence are server-observable properties, provable in-process against the same real database. Found and fixed a genuine, previously-unverified concurrency gap — `createSale`/`createReturn`/`approveReturn`'s replay-safety check raced under truly concurrent requests, the first test in this project's history to exercise that — and found a related gap in Sprint 40's own `seed-tenant.ts` fixture (it never exposed the seeded user's `authUserId`, unusable for any test calling application service code). N-device fuzzed composition (100 runs) written and locally confirmed passing, held back for backlog item 7's nightly wiring. 84/84 integration checks pass; 209 web unit tests unaffected. Sprint 42 closed M4 item 7, the nightly CI pipeline — `nightly.yml` (the N-device fuzzed test, the only nightly-deferred content items 5/6 actually produced) plus Dependabot. Found two documents had been claiming test coverage that was never built: `ci-pipeline.md`'s "full failure-scenario suite" (only 1 of 10 scenarios is server-testable, already PR-gated) and its "extended property-based tests" (no such suite exists on any tier — `test-strategy.md` had cited it for DR-008/DR-013 coverage since Phase 14's first version, corrected to the real unit-test coverage that does exist). Built a standing-issue-on-failure mechanism as a working substitute for the release-candidate gate `ci-pipeline.md §3` specifies, since no release-candidate pipeline exists yet to actually gate. Sprint 43 closed M4 item 8, the OWASP checklist review against the real build — every one of 20 rows re-verified against actual code, not the design docs cited as evidence. Found the most significant issue of this project to date: row-level security is very likely inert for all real production traffic (no `FORCE ROW LEVEL SECURITY` anywhere, no code ever sets the JWT claims RLS policies depend on), flagged for the founder rather than risk a production outage with a blind fix; also found rate limiting was claimed but never implemented. Fixed in the same pass: security headers, and a real audit-log coverage gap first self-identified in Sprint 12 and left open for 15 sprints (3 of 4 stock-movement types plus settings changes had no paired audit entry at all). Four further gaps named for future work (mobile secure storage, local database encryption, customer-data anonymisation, Android release signing). **M4 now has item 9 remaining.** A follow-on cross-cutting pass (Sprint 44) then checked `release-checklist.md`'s actual pilot-ready gate — M4's real exit criterion — against all of Sprints 40–43's findings for the first time, correcting two stale rows (table count/Realtime scope, "all 10" vs. server-testable failure scenarios) and recording an honest conclusion: **this product is not pilot-ready today**, with four checklist rows unresolved. Sprint 45 then closed the second of Sprint 43's two flagged findings — rate limiting, fully specified since Phase 11 but entirely unimplemented — built for the 3 endpoint classes reachable from this codebase (mutating/read/sync-push, a Postgres-backed fixed-window counter, no external service). Found the Auth (sign-in) class is architecturally unreachable from this codebase at all, since sign-in is a direct client call to Supabase Auth that never touches `apps/web`'s own API — a real, named gap needing a Supabase-side configuration check, not code, left open unlike the mutating/read/sync-push classes which carried no such risk. Sprint 46 then closed a third Sprint 43 finding — `privacy.md §4`'s customer-erasure anonymisation, fully designed since Phase 12 but never implemented — built as `POST /customers/{id}/erase` (Owner only), nulling `name`/`phone`, preserving an already-set deactivation date rather than overwriting it, verified against a real database including that a historical sale referencing the erased customer keeps resolving correctly afterward. Found a related gap in the same pass: `deactivated_at` had never been exposed in any customer API response at all. Sprint 47 then closed a fourth Sprint 43 finding, the first mobile-side one — the Supabase session had been sitting in plaintext `SharedPreferences` on Android despite `flutter_secure_storage` already being a dependency; built `SecureLocalStorage`, tested via a genuine in-memory fake rather than mocking a platform channel, `flutter test` 244/244. Sprint 48 then closed the fifth and last genuinely unblocked Sprint 43 finding — the local Drift database had been plain, unencrypted SQLite despite `data-protection.md §3` deciding on SQLCipher since Phase 12; found the anticipated `sqlcipher_flutter_libs` plugin is now a no-op stub, and the real integration is a `package:sqlite3` 3.x native-hooks declaration instead — verified against a real Android debug build (`libsqlcipher.so` confirmed bundled in the APK) and a dedicated test proving unkeyed reads genuinely fail, `flutter test` 252/252. Of Sprint 43's original findings, what remains is either founder-blocked (RLS, still pending confirmation of the real production database role; Android release signing, needs real production credentials) or genuinely out of this session's reach (sign-in rate limiting, a Supabase-side dashboard configuration check; alerting/monitoring infrastructure, intentionally scheduled for Phase 18 per `incident-response.md`, not pulled forward). Sprint 49 then re-checked `release-checklist.md §2` — M4's own actual exit criterion — against Sprints 45–48's real results for the first time since Sprint 44's own pass: the nightly suite has now genuinely fired on its own schedule and passed (confirmed via `gh run list`, not assumed), and the OWASP row's stale "rate limiting unimplemented" wording was corrected to the narrower, accurate sign-in-specific gap. RLS and the 9 unverified failure scenarios were re-confirmed unchanged rather than silently assumed. This product is still not pilot-ready today, but with three unresolved release-gate rows instead of four. Sprint 50 then found that 2 of the 9 offline failure scenarios `test-plan.md §3` had classified as needing new mobile test infrastructure — "App killed mid-sync," "Device rebooted with a full queue" — actually needed none, once attempted: both are built with the existing `flutter test` suite. Found and corrected a real doc/code gap along the way — the mobile client never persists the `Syncing` transitional status `state-machines.md`'s Sync Item diagram specifies; the safety guarantee still holds, via the simpler mechanism actually in code. Narrows (does not flip) the failure-scenarios release-gate row to 5 of 10 still genuinely unverified. Sprint 51 then built the last of the mobile-only rows, "Schema version mismatch after an update," and found what is arguably this project's most severe testing finding to date in the process: `Drift`'s `Migrator.createTable` builds a table from its current Dart shape, not its historical one, so a table created in one migration step (Sprint 37) and altered in a later one (Sprint 39) broke with an unhandled duplicate-column error for any real device jumping both steps in a single update — permanently losing access to its own local database, unsynced sales included. Reproduced live, then fixed with a guarded column check; the standing rule for every future migration is now written down in `schema-local.md`. Narrows the failure-scenarios row again, to 4 of 10 unverified. Sprint 52 then found a third consecutive instance of the same pattern: `test-plan.md` had specifically said the client half of "Connectivity lost mid-batch" needed a live server and fault-injecting proxy, but the actual guarantee — an operation missing from an otherwise well-formed response is left safely untouched — is exercisable with a fake response, no different in kind from the two rows before it. `failure-scenarios.md`'s "return to FailedRetrying" wording was corrected the same way. Only "Storage full" (genuinely unbuilt) and "Token expired while queued" (genuinely needs the full Supabase CLI stack) remain real gaps in the failure-scenario list; the row narrows to 3 of 10 unverified. Sprint 53 then began building storage-full handling — founder-confirmed — and found its foundational piece was never actually built: `inbound-sync.md §4` had decided a `stock_movements` retention window (current + prior financial year) that neither the server pull nor the local cache ever actually enforced, so a device's local transaction history has been growing without bound since the feature first shipped. Both fixed (server-side bound on the server's own clock; local pruning on the device's own clock, a deliberate, reasoned exception since it's a purely local cache-size decision). The fourth consecutive sprint where checking a design claim against the code, rather than trusting it, found a real gap. Sprint 54 then built the last real, buildable gap in the failure-scenario list — storage-full handling — genuinely new product surface rather than a test/fix, so checked with the founder before starting: real free-disk-space detection (`disk_space_2`, chosen after checking pub.dev directly the same way Sprint 48 checked SQLCipher) and the exact low-storage warning `failure-scenarios.md §3` had already designed, now rendered persistently on the home screen. Storage-full is now the 6th of the 10 named failure scenarios with real coverage; only "Token expired while queued" remains, needing infrastructure this project doesn't have. Sprint 55 then closed Sprint 43's other still-open OWASP finding — device revocation, `authorisation-model.md §2`'s step 2, unimplemented since it was first written — building a `devices` table, register/list/revoke endpoints, and a per-request check in `requireSession` via a new `X-Device-Id` header, verified against a real Postgres connection with the same RLS deliberate-break-and-fix rigor Sprint 40 established (98/98 integration checks). Found a real rollout risk before merging — this hard check would reject every request from any currently-installed mobile build — confirmed directly with the founder that no live reliance on today's backend makes this safe to ship now, with mobile-side wiring (the register-device call, the header, handling a revoked session) as the deliberately separate next piece. Sprint 56 then built that deferred next piece — `client_device_id` generated and persisted locally, registered on sign-in and on launch, sent as `X-Device-Id` on every request, a revoked-device response forcing an immediate local sign-out — closing Sprint 43's device-revocation finding in full. Found and fixed two real bugs along the way: a migration test's manually-reconstructed historical schema needed updating for the new column (the same class of gap Sprint 51 found), and a transient register-device failure was letting a successful sign-in surface as a failed one, now swallowed best-effort. Sprint 57 then closed "Token expired while queued," the last of the 10 named offline failure scenarios without real coverage — checking directly whether its own "needs the full Supabase CLI stack" claim held, the fifth such check in this project (after Sprints 50/51/52/54), and finding it false again: the real gap was that no reactive refresh-and-retry code existed in the mobile client at all, despite `authentication.md §3` already implying it did, plus a previously-undocumented server fact (`core/auth/session.ts` never emits a distinct `TOKEN_EXPIRED` code). Built the missing interceptor and corrected the docs to match. All 10 named failure scenarios now have real coverage — `release-checklist.md`'s failure-scenarios row flips to satisfied for the first time, leaving only two unresolved release-gate rows (MTS execution, the OWASP review's RLS/sign-in-rate-limiting findings), both founder-blocked or infra-blocked. Sprint 58 then found a third finding had fallen through a crack between two documents: Android release signing (`owasp-checklist.md`'s M8 finding, open since Sprint 43) had never been threaded into `release-checklist.md`'s actual release gate, and — investigated rather than assumed harmless — genuinely blocks even the pilot gate, since Google Play Console's Internal Testing track (this project's own designed pilot-distribution mechanism) cannot accept a debug-signed build. Found a second, compounding gap in the same pass: the entire Android build→sign→upload CI pipeline `cd-workflows.md §2` describes was never actually built, the same "designed but not built" class of gap Sprint 55/PR #79 already found for that document's §1. Corrected across all three documents, plus a self-introduced accounting error from Sprint 57's own edit. Sprint 59 then went one level deeper on the RLS finding specifically: checked whether every RLS SQL file (`001`–`019`) was actually confirmed applied to the real production database, file by file, and found no documentary confirmation at all for `017`/`018` (`sale_line_items`/`sale_payments`/`return_line_items`, Sprint 40's own zero-RLS fix) or `019` (`devices`, Sprint 55) — a distinct, more severe possibility than the already-known FORCE/role finding, since the app working in production is equally consistent with either explanation. Corrected `owasp-checklist.md`, `cd-workflows.md §1`, and `release-checklist.md` accordingly, and flagged this as the single most action-critical item outstanding — genuinely unknown pending founder verification, unlike every other remaining item which is merely known-and-blocked. Sprint 60 then checked every milestone's own exit criterion against what was actually verified at its closure and found M2 had the same class of gap: its `milestones.md` row requires a physical-reference-device measurement that has never actually happened, and M2's own Sprint 30 closure carried no caveat against this, unlike M0's and M4's honest treatment of an equivalent hardware-dependent exit criterion. Corrected `milestones.md` and `backlog.md §3` with a dated note — the underlying "all 6 items done" claim stands; the gap named is between that and this milestone's own stricter exit-criteria text. Sprint 61 then read `pilot-plan.md` in full for the first time this session and found something that made the founder's remaining task list *shorter*, for once: `release-checklist.md`'s Android-distribution row had never been checked against the pilot's actual shape (2–3 founder-visited shops), and Google Play Console distribution — designed at Phase 15, before Phase 16's own much more minimal pilot approach existed — was never really required for it; only a real signing keystore and a direct sideload install are, the same mechanism already proven across every real-device install this project has done. Corrected `cd-workflows.md`, `release-checklist.md` (moving the full CI/Play-Console pipeline to commercial-launch scope), and `pilot-plan.md` itself (added the missing device-install step). M4's pilot-ready blocking concerns drop from three to two. Sprint 62 then built the first new deliverable of this run of sprints rather than another correction: `supabase/sql/diagnostics/check_rls_status.sql`, a read-only diagnostic the founder can paste directly into the Supabase Dashboard's SQL Editor to answer both the RLS-application question (Sprint 59) and the FORCE/role question (`owasp-checklist.md`'s finding #1) directly against the real production database, in one five-minute action. Sprint 63 closed a second real engineering gap the same way: `apps/mobile/android/app/build.gradle.kts` had signed every release build with the debug keystore since M0 — wired to read real credentials from a gitignored `key.properties` if present (falling back to debug unchanged otherwise, verified with a clean `flutter build apk --debug`), with `key.properties.example` naming the exact `keytool` command needed. Sprint 64 then re-examined the third and final "purely founder-blocked" finding, sign-in rate limiting, and found the same pattern a third time: the mobile client did nothing to throttle repeated failed sign-in attempts, despite that being ordinary engineering work independent of any Supabase setting. Built `SignInController`'s exponential-cooldown attempt throttling (5s–60s, resets on success) — real defense-in-depth, honestly scoped as narrower than the still-open server-side gap, not a substitute for it. All three of the OWASP review's remaining findings now have zero engineering work left, each having turned out to bundle real, buildable work with a genuinely founder-only action once actually re-examined. Asked directly what to do next with no engineering work left, the founder chose to begin M5 prep now rather than wait for M4's remaining founder actions — a dated exception recorded in milestones.md, the same shape as modules/README.md's own M0→M1 exception. Sprint 65 decomposed pilot-plan.md into item grain (backlog.md §6) — M5 has zero engineering items, since its own scope is explicitly "no new product work" — and named which pilot success-metrics are already visible via a built feature (cash reconciliation variance, via Trading Day) versus needing a manual database query during the pilot itself (sync success rate, duplicate sale rate, unresolved sync conflicts). Sprint 66 then triaged the 12 Dependabot PRs Sprint 42 had wired but never processed (9 merged, 1 self-closed as a dead dependency, 3 left open on confirmed real breaking changes) and closed the lowest-risk of the three — `eslint-config-next` 16's `FlatCompat`/circular-JSON crash, fixed by switching `eslint.config.mjs` to the package's own native flat-config exports. Sprint 67 closed the second — Vitest 4 needed `vite` added as an explicit devDependency, since it had only ever resolved transitively and was never pinned. Sprint 68 closed the third and last — Prisma 7, the one with real runtime blast radius: `schema.prisma`'s datasource block lost its connection strings entirely, now split between a new `prisma.config.ts` (CLI/migrate) and an explicit `@prisma/adapter-pg` driver adapter in `core/db/client.ts` (runtime), also fixing a real `@prisma/client`/`prisma` version mismatch Dependabot's own PR had left behind. All 12 Dependabot PRs from the Sprint 65 triage are now resolved. Sprint 69 then re-audited Phase 07's `schema-server.md` against the live schema for the first time since it was written, finding `device_id` documented as a real column on 3 tables that never got it (`trading_days`' whole per-device scoping narrative corrected to the real Sprint 26 design, already reasoned in a module spec but never carried back to this document for 42 sprints), a matching false `client_operation_id` column on 3 more tables, and 3 real built tables never documented at all (`invoice_sequences`, `customer_field_conflicts`, `rate_limit_buckets`). No code change — 4 real unindexed live query paths found in the same pass were named, not fixed, pending a dedicated follow-up sprint. Sprint 70 then re-verified those 4 findings against live query code before building anything and found one was itself a mistake (`sale_line_items(product_id)` — no server query exists, FR-073 is fully offline); built the 2 that were real (`sales(tenant_id, customer_id, completed_at)`, `products(tenant_id, category_id)`) and re-scoped the `products` text-search index to name its actual fix (`pg_trgm`, not the originally-documented plain B-tree form). Sprint 71 then triaged a new weekly Dependabot batch (7 PRs) the same way — 5 merged clean (including two major bumps, `next` 16 and `zod` 4, that turned out non-breaking), 2 left open on confirmed genuine upstream blockers (`typescript` 7.0 needs `@typescript-eslint` support; `eslint` 10.9.0 breaks `eslint-plugin-react`'s rule-context API). Sprint 72 then built `pg_trgm` trigram GIN indexes on `products.name`/`sku` — the real fix Sprint 70 had correctly re-scoped rather than build with the wrong tool — leaving only `sale_line_items.hsn_sac_code_at_sale` from Sprint 69's original findings. Sprint 73 then built that last one — `pos/service.ts#createSale` now snapshots `products.hsn_sac_code` onto each line item at sale-creation time, RR-003's per-line GST field — closing every real, buildable finding from Sprint 69's original audit. Sprint 74 then found `authentication/specification.md` — the single most foundational module spec — unchanged since Sprint 06 (2026-08-02) despite Sprints 23/55/56 building nearly everything it still described as unbuilt across almost all 11 sections; corrected comprehensively, narrowed to the one genuinely remaining gap (a device-list/revocation UI screen, confirmed unbuilt by direct grep, distinct from the now-corrected backend). No code change. Sprint 75 then found the largest gap this whole run of audits has produced: the OpenAPI-driven client/server contract-generation mechanism `shared-contracts.md` designs — meant to be the actual way `apps/web` and `apps/mobile` stay type-consistent — was fully designed at Phase 08 and never built at all, across 74+ sprints and roughly 35 real API endpoints (`packages/contracts/` holds only a stub `package.json`; `docs/11-api/openapi.yaml` has been stale since Sprint 01; never once documented as a decision). Named the real mechanism actually in use (hand-written Zod/Dart per module, kept consistent by this project's own established cross-referencing discipline) and made the explicit judgment call not to build the originally-designed mechanism now, given the scale of already-working production code it would mean migrating — corrected across `input-validation.md`, `mobile-structure.md`, and `monorepo-layout.md` in the same pass. No code change. Sprint 76 then found a real false safety claim rather than mere staleness: `entity-classification.md §3` said `trading_days` was "conflict-free by construction" via per-device scoping — never true, the real (Sprint 26) scoping is per-store, guarded by a real database-level unique index against a genuine concurrent-open race, confirmed directly in the migration file — the same false claim was traced to and corrected in `schema-local.md` and `conflict-resolution.md` too. No code change. See [implementation-log.md](18-implementation/implementation-log.md) for the full per-sprint record. |

**Legend:** ⚪ Not started · 🟡 Draft · 🔵 In review · 🟢 Approved · 🔴 Blocked

---

## Cross-cutting folders

| Folder | Purpose |
| --- | --- |
| [00-governance](00-governance/) | How we work: documentation standards, Definition of Done, review rules. Applies to every phase. |
| [adr](adr/) | Architecture Decision Records. Every architecturally significant decision, with context, options considered, and consequences. Immutable once accepted. |
| [modules](modules/) | Per-module specifications. One folder per business module, authored during Phase 18 immediately before that module is implemented. |
| [reference](reference/) | External research, vendor limits, competitor analysis, regulatory notes. |
| [assets](assets/) | Diagrams, wireframes, exported images referenced by the documents. |

---

## Non-negotiable rules

1. **Design before code.** No implementation begins before its module specification is approved.
2. **One module at a time.** A module is finished — business rules, schema, API, validation, error
   handling, offline behaviour, realtime behaviour, UI, tests, documentation — before the next
   module starts.
3. **No placeholders.** No `TODO`, no stub, no "implement later" in merged code.
4. **Documentation is updated in the same pull request as the code it describes.**
5. **Every architecturally significant decision gets an ADR.** If someone will ask "why is it like
   this?" in six months, it needs an ADR.

---

## Document index

- [Glossary](GLOSSARY.md) — shared vocabulary. Ambiguous terms cause defects; this file prevents them.
- [Documentation Standards](00-governance/documentation-standards.md)
- [Definition of Done](00-governance/definition-of-done.md)
- [Ways of Working](00-governance/ways-of-working.md)
